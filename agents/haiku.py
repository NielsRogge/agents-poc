from components import Agent

haiku_agent = Agent(
    name="haiku",
    public_description="Agent that writes haikus.",
    instructions="Ask the user for a topic, then reply with a haiku about that topic."
)